# MyTwit - Социальная сеть

Готовый скрипт социальной сети в стиле Twitter на React, PHP 8.1 и PostgreSQL. Разворачивается на любом локальном или боевом сервере с Nginx. На данный момент установщика нет, всё полностью вручную.

**🇬🇧 [English version / Английская версия](README.md)**

**📋 [Changelog](CHANGELOG_RU.md)** | **[Changelog EN](CHANGELOG.md)**

## Возможности

### Основные функции
- ✅ Регистрация и авторизация пользователей
- ✅ JWT аутентификация с access/refresh tokens (access token 15 минут, refresh token 30 дней)
- ✅ Автоматическое обновление токенов без разлогинивания
- ✅ SPA без перезагрузки страницы
- ✅ Адаптивный дизайн (десктоп, планшет, мобильный)
- ✅ Тёмная тема с сохранением в БД
- ✅ Мультиязычность (EN, RU) с редактируемыми переводами

### Посты и взаимодействие
- ✅ Создание постов с текстом (до 280 символов)
- ✅ Прикрепление картинок к постам (до 4 на пост)
- ✅ Прикрепление GIF с автоматической конвертацией в MP4
- ✅ Прикрепление видео с HLS-конвертацией (360p/720p/1080p)
- ✅ Кастомный видеоплеер с выбором качества и сохранением громкости
- ✅ Лайки постов
- ✅ Комментарии (ответы и вложенные ответы)
- ✅ Цитирование постов
- ✅ Быстрый ответ без ухода из ленты
- ✅ Счётчик просмотров с дедупликацией по сессии (учитывает просмотры в ленте и на странице поста как один эвент)
- ✅ Динамическое обновление ленты
- ✅ Лайтбокс для медиа с навигацией

### Профили
- ✅ Профили пользователей с био, локацией, сайтом
- ✅ Загрузка аватара
- ✅ Список постов пользователя
- ✅ Редактирование профиля
- ✅ Подписки на пользователей (follow/unfollow), списки подписчиков и подписок
- ✅ Страница настроек аккаунта (смена username, country, gender, language)

### Обработка медиа
- ✅ Автоматическая генерация миниатюр для картинок
- ✅ Конвертация GIF в MP4 через FFmpeg (~96% экономии размера)
- ✅ HLS-транскодинг видео в нескольких качествах (360p/720p/1080p)
- ✅ Автоматическая очистка временных файлов через 6 часов
- ✅ Выбор качества при воспроизведении видео
- ✅ Сохранение громкости в базе данных
- ✅ Глобальный индикатор загрузки медиа

### Безопасность
- ✅ Access/Refresh token система — короткоживущие access tokens (15 мин)
- ✅ Автоматическая ротация токенов — бесшовное обновление без разлогинивания
- ✅ Выход на всех устройствах — возможность отозвать все активные сессии
- ✅ Rate limiting на регистрацию, вход и создание постов
- ✅ IP validation — защита от SQL injection через заголовки прокси
- ✅ Path traversal protection — валидация путей через `realpath()` при удалении файлов
- ✅ CORS whitelist — разрешены только указанные домены (не `*`)
- ✅ JWT secret в .env — криптостойкий секрет не хранится в Git
- ✅ Проверка MIME типа через содержимое файла (не расширение)
- ✅ Ограничения размера файлов (картинки 5МБ, GIF 10МБ, видео 100МБ)
- ✅ Валидация путей FFmpeg против directory traversal
- ✅ Автоматическое удаление медиа при удалении постов

## Технологии

### Frontend
- **React 18** — библиотека UI
- **React Router** — роутинг для SPA
- **Vite** — быстрая сборка и dev-сервер с alias support (`@/`)
- **Axios** — HTTP клиент с автоматическим refresh токенов
- **HLS.js** — библиотека для видеостриминга

### Backend
- **PHP 8.1** — серверная логика
- **PostgreSQL** — реляционная база данных
- **Redis** — дедупликация просмотров и прочие эфемерные данные (в перспективе — presence/typing для чата)
- **JWT** — аутентификация с access/refresh tokens
- **FFmpeg** — обработка видео/GIF

### Веб-сервер
- **Nginx** — веб-сервер (Linux/WSL)
- **PHP-FPM** — FastCGI менеджер процессов PHP

## Системные требования

### Обязательно
- Node.js 16+ и npm
- PHP 8.1+
- PostgreSQL 12+
- Redis 6+
- Nginx
- FFmpeg (для обработки GIF и видео)
- Linux OS (протестировано на WSL Ubuntu)

### PHP расширения
```ini
extension=pdo_pgsql
extension=pgsql
extension=gd
extension=fileinfo
extension=redis
```

Разрешите функции в php.ini:
```ini
disable_functions = 
; Удалите: exec, proc_open, proc_close, proc_get_status
```

## Установка

> **Важно:** инструкции ниже ориентированы на стандартную структуру Linux-сервера (Ubuntu/Debian с Nginx и PHP, установленными через apt, процесс работает от пользователя `www-data`). Если сервер настроен через панель управления (aaPanel/BT-Panel, cPanel, Plesk, ISPmanager и т.п.), пути и названия сервисов будут отличаться — как минимум: расположение конфигов Nginx для доменов, имя systemd-юнита PHP-FPM, путь к `php.ini`, системный пользователь веб-сервера и способ установки PHP-расширений (через pecl вместо apt, если PHP собран панелью из исходников). Уточняйте эквивалентные пути в документации своей панели.

### 1. Клонирование репозитория и установка зависимостей

```bash
git clone <url-репозитория>
cd mytwit
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и **обязательно замените** `JWT_SECRET` на уникальное значение:

```bash
# Генерация криптостойкого секрета (выберите один способ):
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# или
openssl rand -hex 32
# или
php -r "echo bin2hex(random_bytes(32));"
```

Скопируйте сгенерированное значение в `.env`:

```env
JWT_SECRET=ваш_сгенерированный_секрет_здесь
APP_ENV=production
APP_URL=http://ваш-домен.com
CORS_ALLOWED_ORIGINS=http://ваш-домен.com,https://ваш-домен.com
```

⚠️ **Важно:** Никогда не коммитьте `.env` в Git!

### 3. Настройка базы данных

Создайте базу данных и выполните SQL скрипты:

```bash
psql -U postgres
CREATE DATABASE ваша_база;
\c ваша_база
\i database/schema.sql
```

Затем выполните миграции:

```bash
\i database/migrations/001_add_theme_preference.sql
\i database/migrations/002_add_language.sql
\i database/migrations/003_add_profile_fields.sql
\i database/migrations/004_posts_parent_id.sql
\i database/migrations/005_posts_quick_reply.sql
\i database/migrations/006_add_views_count.sql
\i database/migrations/007_unify_post_media.sql
\i database/migrations/008_add_media_thumbnails.sql
\i database/migrations/009_temp_uploads.sql
\i database/migrations/010_add_video_volume.sql
\i database/migrations/011_add_tracking_id_to_temp_uploads.sql
\i database/migrations/012_add_account_info_fields.sql
\i database/migrations/013_add_refresh_tokens.sql
```
### 4. Настройка конфигурации приложения

Отредактируйте файл [`config/config.php`](config/config.php) при необходимости. Основные настройки теперь читаются из `.env`.

### 5. Установка и настройка Redis

Redis используется для дедупликации просмотров постов (и в будущем — для presence/typing-индикаторов чата). Установите сервер и PHP-расширение:

```bash
sudo apt install redis-server
sudo systemctl enable redis-server
sudo systemctl start redis-server

sudo apt install php8.1-redis
sudo systemctl restart php8.1-fpm
```

Проверьте, что всё работает:

```bash
redis-cli ping        # должен ответить PONG
php -m | grep redis   # должен вывести "redis"
```

Если PHP собран не через системный пакетный менеджер (актуально для панелей управления вроде aaPanel), `php8.1-redis` через apt не установится — используйте `pecl install redis` для конкретной сборки PHP и вручную добавьте `extension=redis.so` в `php.ini`.

### 6. Настройка веб-сервера (Nginx)

Создайте конфигурационный файл для вашего домена. На стандартном Ubuntu/Debian сервере конфиги доменов лежат в `/etc/nginx/sites-available/` со ссылкой в `/etc/nginx/sites-enabled/`; в панелях управления (aaPanel, cPanel и т.п.) — в своём собственном месте, конфиг обычно создаётся и подключается через интерфейс панели, а не вручную.

**Пример конфигурации:** см. файл [`nginx.conf`](nginx.conf) в корне проекта

После создания конфигурации (на стандартном сервере):

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/mytwit.com /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

Убедитесь, что PHP-FPM запущен (имя сервиса зависит от способа установки PHP — на стандартном сервере обычно `php8.1-fpm`, в панелях управления может отличаться, например `php-fpm-81`):

```bash
sudo systemctl status php8.1-fpm
sudo systemctl start php8.1-fpm  # если не запущен
```

### 7. Настройка прав доступа к файлам

```bash
# Создайте директории для загрузок
mkdir -p uploads/avatars uploads/posts uploads/posts/thumbs uploads/gifs uploads/videos
```

Права нужно выставить на пользователя, от которого работает PHP-FPM — на стандартном сервере это обычно `www-data`, в панелях управления может быть отдельный системный пользователь (например `www` в aaPanel). Узнать его можно из конфигурации пула PHP-FPM (директива `user` в `php-fpm.conf`/`www.conf`) или из владельца остальных файлов проекта.

```bash
# Пример для стандартного сервера (Ubuntu/Debian):
sudo chown -R www-data:www-data uploads/
sudo chmod -R 755 uploads/
```

Права `755` достаточно, если PHP-FPM работает от того же пользователя, что и владелец папки. Не используйте `777` на постоянной основе — это даёт запись любому процессу на сервере, а не только веб-серверу.

### 8. Настройка cron-задачи для очистки

Добавьте в crontab для очистки старых временных файлов:

```bash
# Редактируйте crontab
crontab -e

# Добавьте строку (выполняется каждые 30 минут, укажите свой путь к проекту):
*/30 * * * * php /путь/к/проекту/cleanup_temp_uploads.php
```

В панелях управления обычно есть собственный интерфейс для cron-задач (например «Плановые задачи» в aaPanel) — через него может быть удобнее, чем через `crontab -e`, так как панель сама подставит корректный путь к PHP-бинарнику.

### 9. Сборка и запуск

```bash
npm run build
```

После сборки откройте ваш домен в браузере. Перезапуск веб-сервера не требуется.

Запускайте `npm run build` после каждого изменения в `src/`.

#### Режим разработки (с hot reload)

Для удобной разработки с авто-перезагрузкой:

```bash
npm run dev
```

Приложение будет доступно по адресу **http://ваш-домен:3333**

## Структура проекта

```
your-project/
├── api/                      # Backend PHP API
│   ├── classes/              # Модели (Database, User, Post, JWT, Redis)
│   ├── controllers/          # Контроллеры (Auth, Post, User)
│   ├── middleware/           # AuthMiddleware, RateLimitMiddleware
│   ├── Router.php            # Роутер
│   └── index.php             # Точка входа API
├── config/                   # Конфигурация
│   └── config.php            # Основной конфиг (читает .env)
├── database/                 # SQL схема и миграции
│   ├── schema.sql            # Начальная схема БД
│   ├── migrations/           # Миграции БД
├── src/                      # Frontend React
│   ├── api/                  # API клиент с auto-refresh токенов
│   ├── components/           # UI компоненты (организованы по фичам)
│   │   ├── compose/          # Компоненты создания постов
│   │   ├── layout/           # Layout, Sidebar, MobileNav
│   │   ├── post/             # Компоненты отображения постов
│   │   └── user/             # Компоненты пользователей
│   ├── context/              # AuthContext, UploadContext, PostsContext
│   ├── i18n/                 # Переводы (EN, RU)
│   ├── pages/                # Страницы приложения
│   │   └── settings/         # Подстраницы настроек
│   └── styles/               # Глобальные CSS стили
├── uploads/                  # Загруженные файлы
│   ├── avatars/              # Аватары пользователей
│   ├── posts/                # Картинки постов
│   ├── posts/thumbs/         # Миниатюры картинок
│   ├── gifs/                 # GIF сконвертированные в MP4
│   └── videos/               # HLS видеопотоки
├── .env                      # Переменные окружения (НЕ коммитить!)
├── package.json
├── vite.config.js            # Конфиг Vite с alias @ → src/
├── nginx.conf                # Пример конфигурации Nginx
└── cleanup_temp_uploads.php  # Cron-задача для очистки временных файлов
```

## Архитектура безопасности

### Access/Refresh Token Flow

```
┌─────────────┐
│   Логин     │
└──────┬──────┘
       │
       ▼
┌────────────────────────────────────┐
│ Сервер генерирует:                │
│ • Access Token  (15 минут)        │
│ • Refresh Token (30 дней, в БД)  │
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ Клиент сохраняет в localStorage   │
└────────┬───────────────────────────┘
         │
         ▼
┌────────────────────────────────────┐
│ Каждый запрос:                    │
│ Authorization: Bearer {access}    │
└────────┬───────────────────────────┘
         │
         ▼
    ╔════════════════════╗
    ║ Access token       ║
    ║ валиден?           ║
    ╚════════╦═══════════╝
         ✅  │  ❌
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌────────┐      ┌──────────────────────┐
│ Ответ  │      │ 401 Unauthorized     │
└────────┘      └──────────┬───────────┘
                           │
                           ▼
                ┌─────────────────────────┐
                │ Frontend автоматически: │
                │ POST /auth/refresh      │
                │ {refreshToken}          │
                └──────────┬──────────────┘
                           │
                           ▼
                    ╔═══════════════╗
                    ║ Refresh token ║
                    ║ валиден в БД? ║
                    ╚═══════╦═══════╝
                        ✅  │  ❌
                            │
                   ┌────────┴────────┐
                   │                 │
                   ▼                 ▼
         ┌──────────────────┐  ┌────────────┐
         │ Новый access     │  │ Logout +   │
         │ token (15 мин)   │  │ Redirect   │
         └────────┬─────────┘  └────────────┘
                  │
                  ▼
         ┌──────────────────┐
         │ Повторить        │
         │ оригинальный     │
         │ запрос           │
         └──────────────────┘
```

**Преимущества:**
- Если украден access token → доступ только 15 минут
- Refresh token хранится в БД → можно отозвать (logout-all)
- Пользователь остаётся залогинен при активном использовании
- Автоматическая ротация токенов прозрачна для пользователя

## Документация

- [API Endpoints](docs/api_endpoints.md)
- [Архитектура постов и ответов](docs/posts_architecture.md)

## Следующие шаги (TODO)

- [ ] Уведомления
- [ ] Личные сообщения
- [ ] Поиск пользователей и постов
- [ ] Хэштеги и упоминания
- [ ] Закладки
- [ ] Списки
- [ ] Верифицированные аккаунты
- [ ] CSRF protection (токены для форм)
- [ ] httpOnly cookies вместо localStorage
- [ ] Страница "Активные сессии" с управлением устройствами

## Лицензия

Все права защищены.

---

**Примечание:** Этот проект требует Linux окружение для полной функциональности. Пользователям Windows следует использовать WSL (Windows Subsystem for Linux).
